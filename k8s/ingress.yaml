---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
spec:
  ingressClassName: nginx
  rules:
    - host: frontend.dev1.p-tishkov.hf.flant.com
      http:
        paths:
          - backend:
              service:
                name: frontend
                port:
                  number: 8000
            path: /
            pathType: ImplementationSpecific
